[project]
name = "rag-search-engine"
version = "0.1.0"
description = "RAG search engine from scratch"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "nltk==3.9.1",
    "numpy==2.5.2",
    "python-dotenv>=1.2.3",
    "sentence-transformers>=6.0.0",
]

[dependency-groups]
dev = [
    "ruff>=0.16.2",
]
